{
  "name": "Высокоточный фрактал Мандельброта через векторизацию 🌀",
  "level": "super_uni",
  "help_text": "Скрипт выполняет параллельный матричный расчет полиномиальной хаотической последовательности на комплексной плоскости с использованием алгоритма сглаживания потенциала для ультра-плавного рендеринга фрактала.",
  "code": [
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "",
    "# 1. Задаем параметры сетки высокого разрешения (1000х1000 точек)",
    "width, height = 1000, 1000",
    "xmin, xmax = -2.0, 0.5",
    "ymin, ymax = -1.25, 1.25",
    "max_iter = 256",
    "escape_radius = 100000.0  # Огромный радиус для идеальной точности формулы сглаживания",
    "",
    "# 2. Формируем комплексную плоскость в виде единой большой матрицы C",
    "x = np.linspace(xmin, xmax, width)",
    "y = np.linspace(ymin, ymax, height)",
    "X, Y = np.meshgrid(x, y)",
    "C = X + 1j * Y",
    "",
    "# Инициализируем матрицы состояний",
    "Z = np.zeros_like(C)",
    "smooth_iterations = np.zeros(C.shape, dtype=float)",
    "mask = np.ones(C.shape, dtype=bool)  # Активная маска вычислений",
    "",
    "print(f'Запущена векторизированная итерация для матрицы: {C.shape}')",
    "",
    "# 3. Главный векторизированный цикл (вычисляем полином Z = Z^2 + C сразу для всех точек)",
    "for i in range(max_iter):",
    "    # Вычисляем шаг полинома только для тех точек, которые еще не улетели в бесконечность",
    "    Z[mask] = Z[mask]**2 + C[mask]",
    "    ",
    "    # Проверяем критерий вылета (модуль комплексного числа превысил escape_radius)",
    "    abs_Z = np.abs(Z)",
    "    just_escaped = (abs_Z > escape_radius) & mask",
    "    ",
    "    # Применяем зубодробительную формулу сглаживания потенциала Рени",
    "    # Она убирает ступенчатость и делает градиент абсолютно непрерывным",
    "    if np.any(just_escaped):",
    "        smooth_iterations[just_escaped] = i + 1.0 - np.log2(np.log(abs_Z[just_escaped]))",
    "        ",
    "    # Обновляем маску: исключаем вылетевшие точки из дальнейших расчетов для ускорения",
    "    mask[abs_Z > escape_radius] = False",
    "    if not np.any(mask): break",
    "",
    "# Точки, которые остались внутри множества Мандельброта, зануляем",
    "smooth_iterations[mask] = 0",
    "",
    "# 4. Ультра-красивая HD визуализация фрактального облака",
    "fig, ax = plt.subplots(figsize=(10, 10), facecolor='black')",
    "",
    "# Используем психоделическую палитру 'twilight_shifted', 'magma' или 'gnuplot2' для неонового свечения",
    "im = ax.imshow(smooth_iterations, cmap='twilight_shifted', extent=[xmin, xmax, ymin, ymax], interpolation='bilinear')",
    "",
    "# Кастомизация графика под футуристичный стиль",
    "ax.set_title('Множество Мандельброта: Векторизированный гладкий рендеринг', color='white', fontsize=12, pad=15)",
    "ax.set_xlabel('Re(Z) — Действительная ось', color='white')",
    "ax.set_ylabel('Im(Z) — Мнимая ось', color='white')",
    "",
    "# Красим оси и сетку в темные тона, чтобы они не отвлекали от красоты",
    "ax.tick_params(colors='gray', labelsize=9)",
    "ax.grid(True, color='#222222', linestyle='--', linewidth=0.5)",
    "",
    "# Настраиваем цветовую шкалу, гармонирующую с черным фоном",
    "cbar = fig.colorbar(im, ax=ax, fraction=0.046, pad=0.04)",
    "cbar.set_label('Потенциал сходимости полиномиального поля', color='white', fontsize=10)",
    "cbar.ax.yaxis.set_tick_params(color='white', labelcolor='white')",
    "",
    "plt.tight_layout()",
    "plt.show()"
  ]
}
