{
  "name": "Зубодробительный метод Ланцоша для разреженных матриц 🧠",
  "level": "university_advanced",
  "help_text": "Алгоритм проецирует огромную разреженную симметричную матрицу на подпространство Крылова, собирая трёхдиагональную матрицу для быстрого поиска экстремальных собственных значений.",
  "code": [
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "import scipy.sparse as sparse",
    "",
    "# 1. Генерируем гигантскую разреженную симметричную матрицу (1000x1000)",
    "n = 1000",
    "np.random.seed(99)",
    "density = 0.01",
    "A_sparse = sparse.random(n, n, density=density, format='csr')",
    "A = (A_sparse + A_sparse.T).toarray()  # делаем её строго симметричной",
    "",
    "# 2. Запускаем итерационный процесс Ланцоша",
    "m = 80  # число шагов (размерность подпространства Крылова)",
    "alpha = np.zeros(m)",
    "beta = np.zeros(m)",
    "v = np.random.randn(n)",
    "v /= np.linalg.norm(v)  # начальный случайный вектор",
    "",
    "v_prev = np.zeros(n)",
    "V_matrix = np.zeros((n, m))  # матрица ортогональных векторов Ланцоша",
    "",
    "for j in range(m):",
    "    V_matrix[:, j] = v",
    "    w = np.dot(A, v)",
    "    alpha[j] = np.dot(w, v)",
    "    ",
    "    if j < m - 1:",
    "        w = w - alpha[j] * v - (beta[j-1] * v_prev if j > 0 else 0)",
    "        beta[j] = np.linalg.norm(w)",
    "        if beta[j] < 1e-10: break  # процесс сошёлся раньше времени",
    "        v_prev = v",
    "        v = w / beta[j]",
    "",
    "# 3. Строим трёхдиагональную матрицу T",
    "T = np.diag(alpha) + np.diag(beta[:-1], k=1) + np.diag(beta[:-1], k=-1)",
    "",
    "# Вычисляем точные собственные числа и приближённые (числа Ритца)",
    "exact_eigenvals = np.linalg.eigvalsh(A)",
    "ritz_eigenvals = np.linalg.eigvalsh(T)",
    "print(f'Истинный спектр матрицы (мин/макс): {exact_eigenvals[0]:.3f} / {exact_eigenvals[-1]:.3f}')",
    "print(f'Приближение Ланцоша (мин/макс): {ritz_eigenvals[0]:.3f} / {ritz_eigenvals[-1]:.3f}')",
    "",
    "# 4. Мега-визуализация спектрального сжатия",
    "fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(15, 6))",
    "",
    "# Левый график: Структура сжатой трёхдиагональной матрицы T",
    "im = ax1.imshow(np.abs(T), cmap='magma', interpolation='none')",
    "ax1.set_title(f'Полученная трёхдиагональная матрица T ({m}x{m})')",
    "ax1.set_xlabel('Индекс столбца')",
    "ax1.set_ylabel('Индекс строки')",
    "fig.colorbar(im, ax=ax1, label='Абсолютные значения элементов')",
    "",
    "# Правый график: Сравнение плотности распределения собственных чисел",
    "ax2.hist(exact_eigenvals, bins=50, alpha=0.5, color='gray', label='Истинный спектр матрицы A (1000 чисел)', density=True)",
    "ax2.vlines(ritz_eigenvals, ymin=0, ymax=0.1, colors='red', alpha=0.7, linewidth=1, label=f'Числа Ритца из матрицы T ({m} окон)')",
    "ax2.set_title('Сравнение полного спектра и приближения Ланцоша')",
    "ax2.set_xlabel('Собственные значения (\\lambda)')",
    "ax2.set_ylabel('Плотность')",
    "ax2.grid(True, linestyle=':', alpha=0.6)",
    "ax2.legend(loc='upper right')",
    "",
    "plt.tight_layout()",
    "plt.show()"
  ]
}
