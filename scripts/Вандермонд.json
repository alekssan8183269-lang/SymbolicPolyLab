{
  "name": "Полиномиальное расслоение пространства через матрицу Вандермонда 🌐",
  "level": "university_advanced",
  "help_text": "Скрипт генерирует циклически неразделимые данные, строит многомерную матрицу Вандермонда для перехода к полиномам 3-й степени и визуализирует разделение данных в 3D пространстве.",
  "code": [
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "",
    "# 1. Генерируем данные в виде двух вложенных колец (неразделимы прямой линией)",
    "np.random.seed(15)",
    "n_samples = 400",
    "r1 = 1.0 + 0.15 * np.random.randn(n_samples // 2)",
    "theta1 = np.random.rand(n_samples // 2) * 2 * np.pi",
    "X1 = np.vstack([r1 * np.cos(theta1), r1 * np.sin(theta1)]).T",
    "",
    "r2 = 2.5 + 0.25 * np.random.randn(n_samples // 2)",
    "theta2 = np.random.rand(n_samples // 2) * 2 * np.pi",
    "X2 = np.vstack([r2 * np.cos(theta2), r2 * np.sin(theta2)]).T",
    "",
    "X = np.vstack([X1, X2])",
    "labels = np.hstack([np.zeros(n_samples // 2), np.ones(n_samples // 2)])",
    "",
    "# 2. Строим обобщенную матрицу Вандермонда для двумерного полинома 2-й степени",
    "# Переводим признаки [x, y] в пространство [x, y, x^2, y^2, x*y]",
    "x_coords = X[:, 0]",
    "y_coords = X[:, 1]",
    "V = np.vstack([x_coords, y_coords, x_coords**2, y_coords**2, x_coords * y_coords]).T",
    "print(f'Размерность исходной матрицы координат: {X.shape}')",
    "print(f'Размерность полученной матрицы Вандермонда: {V.shape}')",
    "",
    "# 3. Находим веса полинома с помощью псевдообратной матрицы (МНК)",
    "# Строим идеальную полиномиальную разделяющую поверхность",
    "target = labels * 2 - 1  # переводим в метки [-1, 1]",
    "weights = np.dot(np.linalg.pinv(V), target)",
    "Z_poly = np.dot(V, weights)",
    "",
    "# 4. Визуализация: показываем, как полином выталкивает данные в 3D",
    "fig = plt.figure(figsize=(14, 6))",
    "",
    "# Левый график: исходное тупиковое состояние на плоскости",
    "ax1 = fig.add_subplot(121)",
    "ax1.scatter(X1[:, 0], X1[:, 1], color='cyan', edgecolor='k', label='Внутренний кластер')",
    "ax1.scatter(X2[:, 0], X2[:, 1], color='magenta', edgecolor='k', label='Внешнее кольцо')",
    "ax1.set_title('Исходные данные на плоскости (2D)')",
    "ax1.set_xlabel('Ось X')",
    "ax1.set_ylabel('Ось Y')",
    "ax1.grid(True, linestyle='--', alpha=0.5)",
    "ax1.legend()",
    "",
    "# Правый график: Магия Вандермонда в 3D",
    "ax2 = fig.add_subplot(122, projection='3d')",
    "ax2.scatter(X[labels == 0, 0], X[labels == 1, 1], Z_poly[labels == 0], color='cyan', edgecolor='k', s=40)",
    "ax2.scatter(X[labels == 1, 0], X[labels == 1, 1], Z_poly[labels == 1], color='magenta', edgecolor='k', s=40)",
    "",
    "# Отрисовываем разделяющую плоскость Z = 0",
    "grid_x, grid_y = np.meshgrid(np.linspace(-3.5, 3.5, 20), np.linspace(-3.5, 3.5, 20))",
    "grid_z = np.zeros_like(grid_x)",
    "ax2.plot_surface(grid_x, grid_y, grid_z, alpha=0.2, color='gray', label='Разделяющая плоскость')",
    "",
    "ax2.set_title('Полиномиальное расслоение в 3D')",
    "ax2.set_xlabel('X')",
    "ax2.set_ylabel('Y')",
    "ax2.set_zlabel('Высота полинома Z')",
    "ax2.view_init(elev=20, azim=45)",
    "",
    "plt.tight_layout()",
    "plt.show()"
  ]
}
