{
  "name": "Алгоритм PageRank: как Google ищет сайты 🌐",
  "level": "university",
  "help_text": "Скрипт строит большую разреженную матрицу переходов для 100 веб-страниц, находит собственный вектор матрицы методом итераций и визуализирует топ-рейтинг сайтов.",
  "code": [
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "",
    "# 1. Моделируем сеть из 100 сайтов (случайная матрица смежности)",
    "np.random.seed(24)",
    "n_pages = 100",
    "links = np.random.rand(n_pages, n_pages) < 0.05  # 5% шанс ссылки между сайтами",
    "np.fill_diagonal(links, 0)  # убираем ссылки сайтов на самих себя",
    "",
    "# 2. Превращаем в стохастическую матрицу переходов (нормализуем по столбцам)",
    "M = links / (links.sum(axis=0) + 1e-10)",
    "",
    "# Добавляем фактор затухания (damping factor d=0.85) против тупиковых сайтов",
    "d = 0.85",
    "M_hat = d * M + (1 - d) / n_pages * np.ones((n_pages, n_pages))",
    "",
    "# 3. Ищем главный собственный вектор степенным методом (марковская цепь)",
    "pagerank = np.ones(n_pages) / n_pages",
    "for _ in range(50):  # 50 итераций для сходимости",
    "    pagerank = np.dot(M_hat, pagerank)",
    "",
    "print(f'Сумма весов PageRank (должна быть 1.0): {pagerank.sum():.2f}')",
    "top_indices = np.argsort(pagerank)[::-1][:10]",
    "print(f'Топ-3 самых авторитетных сайта: {top_indices[:3]}')",
    "",
    "# 4. Визуализация результатов для студентов",
    "fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(15, 6))",
    "",
    "# Левый график: Структура огромной матрицы переходов сайта",
    "im = ax1.imshow(links, cmap='Greys', interpolation='none')",
    "ax1.set_title('Матрица связей интернета (100x100 сайты)')",
    "ax1.set_xlabel('Куда ссылаются')",
    "ax1.set_ylabel('Откуда ссылаются')",
    "",
    "# Правый график: Распределение авторитетности (Топ-20 сайтов)",
    "top_20_idx = np.argsort(pagerank)[::-1][:20]",
    "ax2.bar(range(20), pagerank[top_20_idx], color='royalblue', edgecolor='black')",
    "ax2.set_xticks(range(20))",
    "ax2.set_xticklabels([f'ID {x}' for x in top_20_idx], rotation=45)",
    "ax2.set_title('Топ-20 самых важных страниц (Вес PageRank)')",
    "ax2.set_xlabel('ID веб-сайта')",
    "ax2.set_ylabel('Значимость в сети')",
    "ax2.grid(axis='y', linestyle='--', alpha=0.5)",
    "",
    "plt.tight_layout()",
    "plt.show()"
  ]
}
