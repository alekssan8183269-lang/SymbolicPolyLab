{
  "name": "Матричное экспоненцирование и Хаос Лоренца 🦋",
  "level": "university_advanced",
  "help_text": "Скрипт решает систему дифференциальных уравнений хаотического аттрактора Лоренца через вычисление матричной экспоненты на каждом шаге и строит потрясающий 3D-график траектории.",
  "code": [
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "from scipy.linalg import expm",
    "from matplotlib.collections import LineCollection",
    "",
    "# 1. Параметры хаотической системы Лоренца",
    "sigma, rho, beta = 10.0, 28.0, 8.0 / 3.0",
    "dt = 0.005          # шаг времени",
    "num_steps = 10000   # количество итераций (длина хвоста бабочки)",
    "",
    "# Массивы для хранения координат трехмерного пространства",
    "xs = np.empty(num_steps)",
    "ys = np.empty(num_steps)",
    "zs = np.empty(num_steps)",
    "speeds = np.empty(num_steps) # для красивой градиентной покраски",
    "",
    "# Начальное положение точки в пространстве",
    "current_state = np.array([1.0, 1.0, 1.0])",
    "",
    "# 2. Главный вычислительный цикл через матричную экспоненту",
    "for i in range(num_steps):",
    "    x, y, z = current_state",
    "    xs[i], ys[i], zs[i] = x, y, z",
    "    ",
    "    # Динамическая матрица системы (Якобиан), меняющаяся от координат",
    "    # Она закручивает и деформирует пространство на каждом микро-шаге",
    "    A = np.array([",
    "        [-sigma,  sigma,    0],",
    "        [ rho-z,    -1,   -x],",
    "        [     y,     0, -beta]",
    "    ])",
    "    ",
    "    # Вычисляем матричную экспоненту e^(A * dt) — это ядро высшего линала!",
    "    U_matrix = expm(A * dt)",
    "    ",
    "    # Эволюция системы: умножаем матрицу на вектор текущего состояния",
    "    current_state = np.dot(U_matrix, current_state)",
    "    ",
    "    # Считаем скорость для визуализации (физика процесса)",
    "    speeds[i] = np.sqrt((-sigma*(x-y))**2 + (x*(rho-z)-y)**2 + (x*y-beta*z)**2)",
    "",
    "print(f'Расчет траектории завершен. Количество точек: {num_steps}')",
    "print(f'Финальная точка системы: X={xs[-1]:.2f}, Y={ys[-1]:.2f}, Z={zs[-1]:.2f}')",
    "",
    "# 3. Сложная и ультра-красивая 3D визуализация",
    "fig = plt.figure(figsize=(12, 9))",
    "ax = fig.add_subplot(111, projection='3d')",
    "",
    "# Трюк для создания многоцветной 3D линии: разбиваем траекторию на сегменты",
    "points = np.array([xs, ys, zs]).T.reshape(-1, 1, 3)",
    "segments = np.concatenate([points[:-1], points[1:]], axis=1)",
    "",
    "# Нормализуем скорость, чтобы привязать её к цветовой палитре",
    "norm = plt.Normalize(speeds.min(), speeds.max())",
    "",
    "# Рисуем траекторию с использованием неоновой палитры 'inferno' или 'magma'",
    "for j in range(0, num_steps - 1, 5):  # шаг 5 для ускорения отрисовки",
    "    seg = segments[j:j+5]",
    "    # Плавное изменение цвета от скорости: медленно — синий, быстро — огненно-желтый",
    "    color = plt.cm.inferno(norm(speeds[j]))",
    "    ax.plot(seg[:, 0], seg[:, 1], seg[:, 2], color=color, linewidth=0.8, alpha=0.8)",
    "",
    "# Стилизация под серьезный научный софт",
    "ax.set_title('Хаотический аттрактор Лоренца через Матричное Экспоненцирование', fontsize=14, pad=20)",
    "ax.set_xlabel('Ось X (Турбулентность)')",
    "ax.set_ylabel('Ось Y (Поток)')",
    "ax.set_zlabel('Ось Z (Конвекция)')",
    "",
    "# Делаем фон стильным и темным, чтобы цвета 'горели'",
    "ax.xaxis.set_pane_color((0.95, 0.95, 0.95, 1.0))",
    "ax.yaxis.set_pane_color((0.90, 0.90, 0.90, 1.0))",
    "ax.zaxis.set_pane_color((0.85, 0.85, 0.85, 1.0))",
    "",
    "plt.tight_layout()",
    "plt.show()"
  ]
}
